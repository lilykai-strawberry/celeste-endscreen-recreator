# celeste-endscreen-recreator
CLI tool to recreate Celeste endscreens from the game files

# Build:
idk tbh i let rider figure it out
# Usage:
after building:
`CelesteEndscreenRecreator --meta-yaml=/path/to/meta.yaml --image-atlas=/path/to/image/atlas --output=/path/to/output/file --frame-count=[how many frames do you want to generate`
